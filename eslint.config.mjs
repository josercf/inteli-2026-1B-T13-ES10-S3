// Arquivo mantido por limitação de remoção do sandbox.
// A configuração real do ESLint vive em eslint.config.js (CommonJS).
// ESLint 9 prioriza .js quando ambos existem, então este arquivo é ignorado.
// Pode ser apagado manualmente quando conveniente: rm eslint.config.mjs
export default [];
